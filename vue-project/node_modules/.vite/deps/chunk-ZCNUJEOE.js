import {
  a
} from "./chunk-OAO74KEG.js";
import {
  L
} from "./chunk-DNXRTHCH.js";

// node_modules/@arcgis/core/views/2d/engine/webgl/GroupContainer.js
var r = class extends a {
  constructor() {
    super(...arguments), this._prevFBO = void 0, this.requiresDedicatedFBO = false;
  }
  dispose() {
  }
  doRender(e) {
    const r2 = this.createRenderParams(e), { context: o, painter: s, profiler: n } = r2;
    this._prevFBO = o.getBoundFramebufferObject(), n.recordContainerStart(this.name);
    const i = this._getFbo(r2), a2 = s.getRenderTarget();
    o.bindFramebuffer(i), s.setRenderTarget(i), o.setDepthWriteEnabled(true), o.setColorMask(true, true, true, true), o.setClearColor(0, 0, 0, 0), o.setClearDepth(1), o.clear(o.gl.COLOR_BUFFER_BIT | o.gl.DEPTH_BUFFER_BIT), o.setDepthWriteEnabled(false);
    for (const t of this.children)
      t.beforeRender(e);
    for (const t of this.children)
      t.processRender(r2);
    for (const t of this.children)
      t.afterRender(e);
    s.setRenderTarget(a2), s.releaseFbo(i), o.bindFramebuffer(this._prevFBO), s.beforeRenderLayer(r2, this._clippingInfos ? 255 : 0, this.computedOpacity), i.colorTexture && (o.setStencilTestEnabled(false), o.setStencilWriteMask(0), s.blitTexture(o, i.colorTexture, L.NEAREST)), s.compositeLayer(r2, this.computedOpacity), n.recordContainerEnd();
  }
  createRenderParams(e) {
    return { ...super.createRenderParams(e), blendMode: this.blendMode, effects: this.computedEffects, globalOpacity: 1 };
  }
  _getFbo(e) {
    const { context: t, painter: r2 } = e, { width: o, height: s } = t.getViewport();
    return r2.acquireFbo(o, s);
  }
};

export {
  r
};
//# sourceMappingURL=chunk-ZCNUJEOE.js.map
