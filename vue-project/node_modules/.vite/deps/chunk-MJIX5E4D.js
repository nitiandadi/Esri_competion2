import {
  y
} from "./chunk-MGJMBJDY.js";

// node_modules/@arcgis/core/views/3d/webgl-engine/lib/Indices.js
function n(n2) {
  if (Array.isArray(n2)) {
    if (n2.length < y)
      return n2;
    for (const r of n2)
      if (r >= 65536)
        return new Uint32Array(n2);
    return new Uint16Array(n2);
  }
  if (n2.length < y)
    return Array.from(n2);
  if (n2.BYTES_PER_ELEMENT === Uint16Array.BYTES_PER_ELEMENT)
    return n2;
  for (const r of n2)
    if (r >= 65536)
      return n2;
  return new Uint16Array(n2);
}
function t(n2) {
  const t2 = 3 * n2;
  return t2 <= y ? new Array(t2) : t2 <= 65536 ? new Uint16Array(t2) : new Uint32Array(t2);
}
var e = (() => {
  const r = new Uint32Array(131072);
  for (let n2 = 0; n2 < r.length; ++n2)
    r[n2] = n2;
  return r;
})();
var i = [0];
var f = (() => {
  const r = new Uint16Array(65536);
  for (let n2 = 0; n2 < r.length; ++n2)
    r[n2] = n2;
  return r;
})();
function o(n2) {
  if (1 === n2)
    return i;
  if (n2 < y)
    return Array.from(new Uint16Array(f.buffer, 0, n2));
  if (n2 < f.length)
    return new Uint16Array(f.buffer, 0, n2);
  if (n2 > e.length) {
    const r = Math.max(2 * e.length, n2);
    e = new Uint32Array(r);
    for (let n3 = 0; n3 < e.length; n3++)
      e[n3] = n3;
  }
  return new Uint32Array(e.buffer, 0, n2);
}
function u(n2) {
  if (1 === n2)
    return i;
  if (n2 < y)
    return Array.from(new Uint16Array(f.buffer, 0, n2));
  if (n2 < f.length)
    return new Uint16Array(f.slice(0, n2));
  if (n2 > e.length) {
    const r = new Uint32Array(n2);
    for (let n3 = 0; n3 < r.length; n3++)
      r[n3] = n3;
    return r;
  }
  return new Uint32Array(e.slice(0, n2));
}

export {
  n,
  t,
  o,
  u
};
//# sourceMappingURL=chunk-MJIX5E4D.js.map
